export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  // Public website's own origin — used only to preview/resolve relative
  // image paths (e.g. "/project3.jpg") stored in website content that live
  // on the website's own public/ folder rather than in backend uploads.
  websiteUrl: 'http://localhost:4200',
  appName: 'RAM Projects'
};
